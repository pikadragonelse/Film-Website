import React from 'react';
import './index.scss';
import { Logo } from '../../asset/icon/logo';

export const Header = () => {
    return (
        <div>
            <Logo />
        </div>
    );
};
